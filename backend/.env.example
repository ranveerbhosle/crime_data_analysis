PORT=5000
MONGODB_URI=mongodb://localhost:27017/crimelens
ML_SERVICE_URL=http://localhost:5001

# Optional: absolute or project-relative paths (defaults match data/ filenames below)
# INDIA_CRIME_CSV=data/crime_dataset_india (1).csv
# GLOBAL_CRIME_CSV=data/global.csv
# Or use Excel (first sheet is imported; large files need enough RAM):
# GLOBAL_CRIME_CSV=data/global_crime.xlsx

# Set to 1 to clear and re-import BOTH India + global (large).
# FORCE_REIMPORT=0

# Set to 1 to clear and re-import ONLY global_crimes (keeps India collection).
# FORCE_REIMPORT_GLOBAL=0
